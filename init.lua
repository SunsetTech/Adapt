local Adapt; Adapt = {
	Execution = require"Adapt.Execution";
	Transform = require"Adapt.Transform";
	Copy = require"Adapt.Copy";
	Process = require"Adapt.Process";
	Sugars = require"Adapt.Sugars";
}; return Adapt;
